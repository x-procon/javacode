<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper:根标签，namespace：命名空间，随便写，一般保证命名空间唯一 -->
<mapper namespace="userDao">
    <!-- statement，内容：sql语句。id：唯一标识，随便写，在同一个命名空间下保持唯一
       resultType：sql语句查询结果集的封装类型,tb_user即为数据库中的表
     -->
    <select id="selectUserById" resultType="com.hazer.gyo.hazer.entity.User" parameterType="int">
        select * from user where id = #{id}
    </select>

    <select id="selectAll" resultType="com.hazer.gyo.hazer.entity.User">
        select * from user
    </select>

    <insert id="insertUser" parameterType="com.hazer.gyo.hazer.entity.User">
        insert user (username,password,name,age,sex,birthday,created,updated)
        values (#{userName},#{password},#{name},#{age},#{sex},#{birthday},now(),now())
    </insert>

    <update id="updateUser" parameterType="com.hazer.gyo.hazer.entity.User">
        update user set username=#{userName} where id = #{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user where id = #{id}
    </delete>

</mapper>
